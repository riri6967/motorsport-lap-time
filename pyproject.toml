[project]
name = "motorsport-lap-time"
version = "0.1.0"
description = "Car-agnostic lap-time and racing-line optimisation engine"
requires-python = ">=3.10"
dependencies = ["numpy>=1.21", "scipy>=1.8", "pyyaml>=5.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
